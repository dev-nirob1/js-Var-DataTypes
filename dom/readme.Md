# The 3 Musketeers of web dev 
- Html (Structure)
- CSS (Style)
- JS (Logic)

# Window Object 
- The window object represents an open window in a browser. It is browser's object (not Javascripts) & is authomatically created by browser.

- It is a glowbal object with lots of properties & methods

# What is DOM
- When a web page is loaded, the browser creaetes a (Document Object Model - DOM) of the page
